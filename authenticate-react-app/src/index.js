import React from "react";
import ReactDOM from "react-dom";
// Import the 'App' component if it's defined in a separate file
import App from "./App"; 

ReactDOM.render(<App />, document.getElementById("root"));
          